#include "main.h"
/**
 * _isupper - checks for uppercase character.
 *
 * @c: uppercase character
 * Return: to 1 if it is upper, else 0
 */
int _isupper(int c)
{

		if (c >= 65 && c <= 90)
			return (1);


		return (0);
	}
